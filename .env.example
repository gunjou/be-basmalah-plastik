# Flask
FLASK_ENV=production
FLASK_APP=api

# JWT
JWT_SECRET_KEY=your_jwt_secret_here
JWT_ACCESS_TOKEN_EXPIRES=72000  # dalam detik (20 jam)

# Database (jika pakai database terpisah, misalnya PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=kasir_db
DB_USER=kasir_user
DB_PASSWORD=kasir_password

# CORS (opsional)
CORS_ORIGINS=http://localhost:3000,http://basmalahplastik.shop

# App settings (opsional)
APP_NAME=Basmalah Plastik

